<%- include("partials/header.ejs") %>
    <section class="vh-100 gradient-custom">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col col-xl-10">

                    <div class="card">
                        <div class="box" id="heading">

                            <% if(locals.setup && locals.delivery && locals.flags){ %>
                                <h3 class="py-1 px-3">
                                    <%= setup %>
                                </h3>
                                <h1 class="py-2 px-3">
                                    <%= delivery %>
                                </h1>
                                <% if( !flags===undefined || flags.length !=0){ %>
                                    <p>
                                        Joke type: <%= flags %>
                                    </p>
                                    <% } %>
                                        <% } else{ %>
                                            <h1>
                                                No Joke, Get better.
                                            </h1>
                                            <% } %>


                        </div>
                        <!-- <div class="card-body p-5">
                            <form action="/add" method="post"
                                class="d-flex justify-content-center align-items-start mb-4">
                                <div class="form-outline flex-fill">
                                    <input type="text" id="form2" class="form-control" name="todoTitle" />
                                    <label class="form-label" for="form2">Add New Joke...</label>
                                </div>
                                <button type="submit" class="btn btn-info ms-2">Add</button>
                            </form>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" value="" id="defaultCheck1">
                                <label class="form-check-label" for="defaultCheck1">
                                    Default checkbox
                                </label>
                            </div>

                            Tabs content
                            <div class="tab-content" id="ex1-content">


                                <div class="tab-pane fade show active" id="ex1-tabs-2" role="tabpanel"
                                    aria-labelledby="ex1-tab-2">
                                    <ul class="list-group mb-0">

                                    </ul>
                                </div>
                            </div>
                            Tabs content

                        </div> -->
                    </div>

                </div>
            </div>
        </div>
    </section>
    <%- include("partials/footer.ejs") %>